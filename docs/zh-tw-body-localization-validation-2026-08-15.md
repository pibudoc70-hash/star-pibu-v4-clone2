# 중국어 번체 상세 본문 현지화 검증 기록

## 범위

`equipment3`의 활성 상세 **72개**에 대해 한국어 원문을 기준으로 대만 번체 현지화를 수행했다. 번체 전용 필드는 `descZhTw`, `detailZhTw`, `effectZhTw`, `cautionZhTw`, `sessionsZhTw`, `timeZhTw`, `recoveryZhTw`이며, 기존 간체 필드를 덮어쓰지 않는다.

## 데이터·품질 검증

| 항목 | 결과 |
|---|---:|
| 대상 상세 | 72개 |
| 현지화 필드 | 7개/상세 |
| 비교 행 수 | 504개 |
| 원문 수치 토큰 보존 오류 | 0건 |
| 한글 잔존(본문 3필드) | 0건 |
| 간체 전용 문자·보증/최상급 표현 자동 검증 오류 | 0건 |
| 모든 번체 필드가 채워진 상세 | 71개 |
| 원문부터 `sessions`가 비어 있는 상세 | 써마지 FLX 1개 |

써마지 FLX의 `sessions`는 한국어 원문도 공란이어서 임의 시술 횟수를 만들지 않았다. 공개 UI와 서버 프리렌더는 번체 전용 필드가 비어 있는 경우에만 기존 간체·영문·한국어 순으로 안전하게 폴백한다.

## 현지화 기준

대만 독자를 기준으로 `皮膚科`, `療程`, `雷射`, `諮詢`, `恢復期` 등의 현지 표현을 적용했다. 모든 숫자·파장·시간·횟수·회복 기간은 원문 토큰을 보존했고, 장비명은 기존 `nameZhTw` 표기를 유지했다. 원문에 포함된 절대적·보증형 문구는 의료광고 준수를 위해 개인차와 적합성 상담을 전제로 한 중립 안내로 전환했다.

## 공개·프리렌더 검증

`/zh-tw/equipment3/써마지-flx` 개발 미리보기에서 전용 번체 본문, 정보 표 라벨, FAQ와 외부 예약 CTA를 확인했다. 서버 프리렌더 회귀 테스트는 번체 전용 본문이 표·메타 설명·`MedicalProcedure`에 간체보다 우선 사용되는지 검증한다.

## 산출물

의료진 검수용 원문-번체 대조표는 `/home/ubuntu/Downloads/star-pibu_zh_tw_body_comparison_2026-08-15.csv`에 생성했다.

## 운영 메모

로컬 Vite production build는 6,779개 모듈의 청크 렌더링 단계에서 샌드박스 메모리 제한으로 세 차례 `SIGTERM(143)`이 발생했다. TypeScript 및 전체 Vitest는 통과했으며, 체크포인트 배포 산출물로 운영 반영을 검증한다.
