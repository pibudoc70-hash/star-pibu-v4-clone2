# 중국어 번체 FAQ 번역 패키지 검증 기록

## 전수 입력 검수

사용자가 제공한 12개 JSON 배치는 **72개 상세와 279문항**을 포함한다. 독립 검증 스크립트는 JSON 구조, ID·slug, 문항 순서, 한국어 원문 대비 문항 수, 한글·간체 잔존, 수치, 개인차 표현, `BBL緊膚` 기준을 검사했다. 장비명 속 세대·모델 숫자와 한국어 서수 표현을 임상 수치와 구분한 뒤 최종 오류는 0건이었다.

## 첫 배치 반영 검증

첫 배치의 6개 상세·26문항을 먼저 트랜잭션으로 저장한 뒤 DB를 확인했다. 모든 상세의 `faqsZhTw` 문항 수가 한국어 원문과 일치했고 한글 잔존 페이지는 없었다. 이후 나머지 11개 배치도 같은 ID·slug·문항 수 조건을 통과한 후 순차 반영했다.

## 공개 상세 확인

` /zh-tw/equipment3/bbl-스킨타이트` 미리보기에서 번체 FAQ 6문항과 `FAQPage` 렌더링, 위치·진료시간·전문의·통증 관리 고정 인용 블록을 확인했다. FAQ 안의 `BBL緊膚`는 정상 표시된다.

> 장비 상세 제목과 기존 본문은 현재 `nameZh`·`descriptionZh` 등 간체 필드를 재사용하므로, 공개 제목은 `BBL紧肤`로 표시되고 일부 기존 본문 라벨도 간체로 남는다. 제공된 `treatmentNameZhTwSuggestion` 72개를 적용하려면 간체 사이트에 영향을 주지 않는 별도 `nameZhTw` 필드와 언어 분기 구현이 필요하다.

## 번체 전용 장비명 적용 후 공개 확인

`nameZhTw` 열과 `/zh-tw` 선택 로직을 추가한 뒤 같은 BBL 상세를 다시 확인했다. 공개 제목·히어로·FAQ·MedicalProcedure 이름은 모두 **BBL緊膚**으로 표시되며, 중국어 간체 `nameZh` 값 **BBL紧肤**는 변경하지 않았다. 패키지에 포함되지 않은 기존 상세 본문·요약·라벨은 여전히 간체 `detailZh`·`effectZh`·`cautionZh` 필드를 재사용한다. 이는 FAQ·장비명 번체 현지화 범위 밖의 기존 콘텐츠이며, 별도 번체 본문 번역 패키지가 필요하다.

## 지역 언급 확대 후 공개 확인

- 한국어 BBL 스킨타이트 상세의 첫 FAQ 답변에는 `부산 서면 스타피부과` 상담 안내가 자연스럽게 추가됐으며, FAQ·고정 인용 블록·카카오 상담·네이버 예약 CTA가 유지됐다.
- 대만 번체 BBL 상세의 제목은 `BBL緊膚`로 표시됐고, 첫 FAQ의 `釜山西面Star皮膚科` 안내와 번체 고정 인용 블록도 정상 렌더링됐다.
- 번체 상세의 기존 시술 소개·효과·주의사항 등 일반 본문에는 별도의 `descriptionZhTw` 필드가 없어 일부 간체가 남는다. 이번 범위는 FAQ·고정 인용 블록·장비명으로 한정하며, 본문 전체 번체화는 별도 콘텐츠 마이그레이션 과제다.
